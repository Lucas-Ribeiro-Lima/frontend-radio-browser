import { AlignJustifyIcon, CheckIcon, ChevronDownIcon, LoaderIcon, PencilIcon, PlayCircleIcon, PlusCircleIcon, SearchIcon, StopCircleIcon, Trash2Icon } from "./icons";

export const Icons = {
  Search: SearchIcon,
  Play: PlayCircleIcon,
  Stop: StopCircleIcon,
  Trash: Trash2Icon,
  More: AlignJustifyIcon,
  Plus: PlusCircleIcon,
  Pencil: PencilIcon,
  Check: CheckIcon,
  Loading: LoaderIcon,
  ChevronDown: ChevronDownIcon
}