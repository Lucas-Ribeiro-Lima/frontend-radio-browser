import { StationCurrentAction, StationCurrentContent, StationCurrentHeader, StationCurrentWrapper } from "./station";

export const Station = {
  Wrapper: StationCurrentWrapper,
  Header: StationCurrentHeader,
  HeaderAction: StationCurrentAction,
  Content: StationCurrentContent,
}
